export enum TypeRoleWorker {
    ADMIN = 1,
    STAFF = 0,
}

export enum TypeStatusOrder {
     WAIT_CONFIRM = 1,
     CONFIRMED = 2,
     PREPARING_ORDER = 3,
     SHIPPING = 4,
     SUCCESSFUL_DELIVERY = 5,
     RECEIVED_MONEY = 6,
     CANCEL_ORDER = 7,
}